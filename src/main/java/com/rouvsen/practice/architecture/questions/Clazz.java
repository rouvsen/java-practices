package com.rouvsen.practice.architecture.questions;

public class Clazz {
    /*
    ClassNotFoundException occurs when you try to load a class at runtime
    using Class.forName() or loadClass() methods and requested classes not found in classpath.
    This error occurs when you do not have corresponding Jar on classpath.
    It is a checked exception.

    NoClassDefFoundError occurs when the class was present during compile time
    and the program was compiled successfully but the class was not present during runtime.
    It is an Error. May be compile your project and then deleted one of the .class file.
    It will give NoClassDefFoundError.

    */
}
